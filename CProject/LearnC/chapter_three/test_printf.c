/**
 * Author@ Cheng Feitian
 * Date: 2020-1-15
 * Description: 测试printf的功能
 */

#include <stdio.h>
#include <string.h>

#define MAXSIZE = 1024

int main(void) {

    /* printf()语句把输出发送到一个叫作缓冲区的中间存储区域，然后缓冲区中的内容再不断的被发送到屏幕上。
     * 1.缓冲区满
     * 2.遇到换行字符
     * 3.需要输入的时候    
     */

    printf("Hello World!");
    while (1)
      


    return 0;
}
